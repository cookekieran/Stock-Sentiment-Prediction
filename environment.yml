name: stocks_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - pyarrow
  - numpy
  - matplotlib
  - scikit-learn
  - scipy
  - sqlalchemy
  - requests
  - pip
  - pytorch
  - ipykernel
  - pip:
      - alpaca-py
      - beautifulsoup4
      - numba
      - pydantic
      - python-dotenv
      - sentencepiece
      - shap
      - tqdm
      - yfinance
      - psycopg2-binary
      - transformers==4.38.2
      - peft==0.10.0